%p#notice= notice
%p
  %strong Событие:
  = @todolist.title.html_safe
%p
  %strong Подробнее о событии:
  = @todolist.description.html_safe
%p
  %strong Автор:
  = User.find(@todolist.user_id).username
%p
  %strong Исполнитель:
  = User.find(@todolist.performer_id).username

%p
  %strong Начало события:
  =I18n.l @event.starts_at, format: :short

%p
  %strong Завершение события:
  =I18n.l @event.ends_at, format: :short


- if current_user.present? && current_user.id == @todolist.user_id
  = link_to edit_todolist_path(@todolist), class: "btn btn-sm btn-warning" do
    %i.fa.fa.fa-pencil.fa-fw
    Редактировать
  = link_to todolist_path(@todolist), method: :delete, data: { confirm: "Точно удалить?" }, class: "btn btn-sm btn-warning" do
    %i.fa.fa-trash-o.fa-fw
    Удалить

= link_to todolists_path, class: "btn btn-sm btn-warning" do
  %i.fa.fa-home.fa-fw
  На главную